export default function createTodo(title, description, date, priority, cheked, id){


    const isFromToday = () =>{}
    const isFromThisWeek= () =>{}
    const isImportant= () => {}
    const isCompleted= () => {}

    return{
        title,
        description,
        date,
        priority,
        cheked,
        id,
        isFromToday,
        isFromThisWeek,
        isImportant,
        isCompleted

    }
} 